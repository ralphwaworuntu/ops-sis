# Salin ke shell sebelum menjalankan install.sh (non-interaktif)
# export $(grep -v '^#' deploy/env.install.example | xargs)

DOMAIN=sis.example.com
EMAIL=admin@example.com
APP_DIR=/var/www/ops-sis
APP_USER=ops-sis
NODE_MAJOR=22
PORT=3000
# INSTALL_FROM_GIT=https://github.com/org/ops-sis.git
# GIT_BRANCH=main
# SKIP_SEED=1
# SKIP_SSL=1
