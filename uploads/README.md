## Folder `uploads/` (lokal)

Folder ini dipakai untuk menyimpan hasil upload (foto LHP, dll) saat running lokal.

- **Tidak dipush ke GitHub** untuk menghindari kebocoran data/PII dan membengkakkan repo.
- Struktur folder tetap disertakan agar aplikasi langsung siap dipakai.

